🔹 1️⃣ Importing Libraries
import pandas as pd
import numpy as np
from datetime import datetime, timedelta
import random

What this does:

pandas (pd) → used to create tables (DataFrames), clean data, group, analyze

numpy (np) → used for random data generation and numeric operations

datetime, timedelta → used to generate realistic dates and times

random → used to randomly pick date formats and values

np.random.seed(42)

Why this matters:

Ensures reproducibility

Every time you run the script, you get the same dataset

Very important for debugging and portfolio consistency

🔹 2️⃣ Configuration (Dataset Size)
N_CUSTOMERS = 10000
N_ORDERS = 50000
N_EVENTS = 200000

What this means:

You’re simulating a large, realistic system

Enough data to:

Stress-test Pandas

Perform funnel analysis

Show performance handling

🔹 3️⃣ Customers Table Creation
customer_ids = [f"C{str(i).zfill(5)}" for i in range(1, N_CUSTOMERS + 1)]

Explanation:

Generates IDs like:

C00001, C00002, ..., C10000


zfill(5) ensures consistent formatting (important for joins)

customers = pd.DataFrame({


Creates a DataFrame called customers.

🔸 signup_date
(datetime(2023, 1, 1) + timedelta(days=random.randint(0, 600)))


Starts from Jan 1, 2023

Adds a random number of days (up to ~2 years)

.strftime(random.choice(["%Y-%m-%d", "%d/%m/%Y", "%m-%d-%Y"]))


Converts dates into different formats

Purpose: simulate real-world messy date data

👉 This forces you to practice date cleaning

🔸 acquisition_channel
np.random.choice([...], p=[0.4, 0.3, 0.2, 0.1])


Randomly assigns acquisition channels

Weighted probabilities:

Organic most common

Referral least common

🔸 age
list(range(18, 65)) + [None]


Ages 18–64

Some values are None (missing)

Used to practice:

Missing value detection

Filling or excluding nulls

customers.to_csv("customers.csv", index=False)


Saves the table as a CSV

index=False avoids adding an extra column

🔹 4️⃣ Orders Table Creation
Order IDs
[f"O{str(i).zfill(6)}"]


Creates IDs like:

O000001, O000002

order_value
np.random.uniform(10, 500)


Random order values between $10 and $500

Rounded to 2 decimal places

discount
[0, 5, 10, 20, None]


Some orders have:

No discount

Small discounts

Missing discounts (None)

Used to demonstrate:

fillna(0)

Discount impact analysis

order_status
["Completed", "Cancelled", "Returned"]


Most orders are completed

Some are cancelled or returned

Realistic business logic

orders.to_csv("orders.csv", index=False)

🔹 5️⃣ Funnel Events Table (Most Important)

This simulates clickstream / user behavior data.

event_type
["visit", "view_product", "add_to_cart", "checkout", "purchase"]


Represents the customer funnel stages.

Probabilities ensure:

Many visits

Fewer purchases

event_time

Random timestamps

Mixed formats:

"2023-03-10 14:22:10"

"10/03/2023 14:22"

This forces datetime standardization.

is_valid
np.random.choice([1, 0], p=[0.95, 0.05])


5% invalid events

Used to show:

Data quality checks

Filtering bad data

events.to_csv("funnel_events.csv", index=False)

🔹 6️⃣ Storage Size Calculation
import os

os.path.getsize(file) / (1024*1024)


Converts bytes → MB

Shows:

File size

Storage footprint

Performance awareness

🔹 7️⃣ Funnel Calculation
events[events["is_valid"] == 1]


Removes invalid events

.groupby("event_type")["customer_id"].nunique()


Groups by funnel stage

Counts unique customers

Prevents double counting

conversion = funnel / funnel.max() * 100


Calculates conversion percentage

Top stage = 100%

🔹 8️⃣ Data Cleaning
orders["discount"] = orders["discount"].fillna(0)


Replaces missing discounts with 0

Prevents incorrect revenue calculations

🔹 9️⃣ Funnel Order Standardization
event_order = {...}


Assigns numeric order to funnel stages

events["event_rank"] = events["event_type"].map(event_order)


Converts text → numeric

Allows:

Sorting

Funnel validation

Sequence checks

🔹 🔟 Visualization (Matplotlib)
plt.plot(funnel.index, funnel.values)


X-axis → funnel stages

Y-axis → number of unique customers

plt.title("Customer Funnel")
plt.xlabel("Stage")
plt.ylabel("Unique Customers")


Adds clarity and professionalism.

🎯 How You Explain This in an Interview

“I generated a realistic multi-table e-commerce dataset, cleaned missing and invalid data, standardized event sequences, calculated funnel conversion rates using unique customers, measured storage size, and visualized customer drop-offs using Matplotlib.”

That answer alone sounds very strong.